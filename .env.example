# OpenClaw Docker Configuration
# Copy this file to .env and fill in the values:
#   cp .env.example .env

# =============================================================================
# Gateway Settings
# =============================================================================

# Authentication token for the web UI (auto-generated by docker-setup.sh)
OPENCLAW_GATEWAY_TOKEN=

# Bind mode: "lan" exposes the gateway on all interfaces (required for Docker)
OPENCLAW_GATEWAY_BIND=lan

# =============================================================================
# Volume & Mount Settings
# =============================================================================

# Named Docker volume for /home/node persistence
OPENCLAW_HOME_VOLUME=openclaw-home

# Additional host directories to mount (comma-separated)
# Format: host_path:container_path:mode
# Example: /home/user/documents:/home/node/documents:ro,/home/user/code:/home/node/code:rw
OPENCLAW_EXTRA_MOUNTS=

# =============================================================================
# Build-time Settings
# =============================================================================

# Additional system packages to install at build time (space-separated)
# Example: ffmpeg imagemagick
OPENCLAW_DOCKER_APT_PACKAGES=

# =============================================================================
# Model Provider API Keys (configure at least one)
# =============================================================================

# Anthropic (Claude)
ANTHROPIC_API_KEY=

# OpenAI
OPENAI_API_KEY=

# Google AI (Gemini)
GOOGLE_AI_API_KEY=

# Ollama (local models - set base URL if using remote Ollama)
# OLLAMA_BASE_URL=http://host.docker.internal:11434
